#include <stdio.h>

int fail = 0;
for(int i = 0; i < N; i++) {
    if(a[i] <= a[i+1]) {
        
    }
    else {
        return false
    }
}

boolean(int arr[]) {
    for(int i = 0; i < N; i++) {
        if(a[i] > a[i+1]) {
            return false;
        }
    }
    return true;
}
